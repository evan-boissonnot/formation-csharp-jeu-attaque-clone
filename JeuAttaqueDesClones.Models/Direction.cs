using System;
using System.Collections.Generic;
using System.Text;

namespace JeuAttaqueDesClones.Models
{
    /// <summary>
    /// Direction de déplacement (gauche, droite, en haut, en bas)
    /// </summary>
    public enum Direction
    {
        Haut = 0,
        Bas = 1,
        Droite = 2,
        Gauche = 3
    }
}
